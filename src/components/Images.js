import React from 'react';
import PropTypes from 'prop-types';

const Images = props => {
  return (
    <div>
      <h1>Images</h1>
    </div>
  );
};

Images.propTypes = {};

export default Images;
